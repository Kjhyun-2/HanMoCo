1) 5635 생일
- 이차원배열에 입력값을 모두 문자열로 입력받은뒤, 숫자부분만을 for문을 사용하여 int형으로 바꿔줌
- 파이썬의 sorted를 사용하고, 년도, 월, 일을 순으로 key=lamda x를 사용
- lamda x에 년도, 월,일 순으로 기준으로 하여 정렬
- 결과적으로 년도빠른순 -> 월 빠른순 -> 일 빠른순으로 정렬 가능

2) 11656 접미사 배열
- 입력받은 문자열을 새로운 변수 l 을 사용하여 l=0, 즉 문자열 처음부터 끝까지, 다음은 l=1즉, 문자열 1번째부터 끝까지 문자열이 끝날때 까지 l을 증가시키면서 반복
- 한글자씩 줄이며 만든 리스트를 2차원배열에 append
- 이차원배열에 sort를 사용하여 사전순으로 정렬

3) 14494 다이나믹이 뭐예요?
- 0,0 부터 시작하는 2차원배열에서 arr[0][0]을 1로 초기화
- x,y로 가는 방법을 나열해 보면서 규칙을 찾음
- arr(i,j)까지 가는 방법은 arr[i-1][j] + arr[i][j-1] + arr[i-1][j-1] 이란 규칙을 갖고 있음
- 이러한 규칙을 사용해서 for문으로 구해줌

4) 1817 짐 챙기는 숌
- 박스를 세는  cnt변수와, 박스 배열 선언
- 무게를 리스트로 받는데, 0번째를 0으로 초기화 한 후 입력 받음
- 무게를 계산할 때 최대 무게를 넘기면 안되기 때문에,i번째 무게를 넣고 최대무게가 안넘는다면, i+1번째 무게를 더한 값이 최대값이 안넘는지 확인
- 넘긴다면, 박스 배열을 초기화한후 cnt를 +1, 만약 안넘는다면 다음 번째 무게를 넣고 비교
