===============================================================================================================================================
5월 21일 (일요일)

1) 1735 분수 합
- x분자와 x분모를 입력받고 y분자와 y분모를 입력한다. 분수끼리 덧셈을 하려면 우선 통분을 해줘야하기때문에 답 result분모변수와 result분자변수를 각각 만든다.
- result분모=>r분모 result분자=>r분자  r분모 = x분모 * y분모를 해서 분모를 서로 같게 만들어준다.
- r분자는 (x분자*y분모)+(x분모*y분자)로 통분해서 저장해둔다.
- 이때 r분모 r분자를 기약분수로 약분해주면 결과값이 나오는데 한번에 기약분수로 만들어주려면 각각 분모와 분자를 최대공약수로 나누어 주는것이다.
- 최대공약수를 구하는 방법으로는 유클리드호제법이 존재한다. 이는 분모를 분자로 나눈 나머지값은 분모가 되고
- 분모와 분자의 값을 서로 바꿔줬을때 분자가 0이 되면 그때의 분모의 값이 최대공약수가 되는것이다. 즉 r분모 = r분모를 r분자로 나눈 나머지값
- r분모와 r분자값을 서로 바꿈 -> r분자값이 0이면 r분모는 최대공약수 만약 0이 아니라면 0이 될때까지 이를 반복하는 것이다.
- 마지막에는 처음 약분하기 전의 r분모와 r분자값에 최대공약수로 나눈값을 출력한다. 유클리드 호제법을 사용하지않으면 최대공약수를 구하는데
- 일일이 공약수를 찾게되므로 시간초과가 났을것으로 예상된다.

2) 1010 다리 놓기
- 이 문제는 확률과 통계단원의 간단한 조합 문제라고 할 수 있다. n개중 r개를 뽑는 방법의 수를 구하는 문제로 nCr로 개산을 한다면
- 쉽게 해결할수있다. nCr의 공식은 다음과 같다 n! / r!(n-r)! 강동쪽 다리가 강서쪽 다리보다 항상 많기 때문에 이때 강서쪽은 r
- 강동쪽은 n이 된다. 이 r과 n 강동, 강서를 저 식에 대입하여 푼다면 쉽게 답이 나올수 있다.

3) 1049 기타줄
- 이 문제는 낱개로 사는 것과 6개 패키지로 사는 것중 뭐가 값이 더 싼지 계산하여 최솟값을 출력하는 문제였다.
- 우선 사야하는 기타줄의 개수를 입력받고 기타줄을 살수있는 브랜드 개수를 입력받았다. 그 이후로 패키지 가격과 낱개 가격이 브랜드별로 입력되는데
- 나는 브랜드중 패키지 가격과 낱개 가격이 가장 싼 값을 찾기위하여 이 입력되는 값들을 다 배열로 저장한 뒤 낱개와 패키지 배열을 각각 정렬하였다.
- 그러면 패키지 배열 0번째와 낱개 배열 0번째에 가격이 가장 싼 값이 저장되므로 이 두개의 값을 최종 패키지 최솟값과 낱개 최솟값 변수에 저장한후 시작하였다.
- 우선 사야하는 기타줄의 개수가 0 or 0보다 작아질 때까지 무한 반복문을 사용하였다. 우선 패키지가 낱개로 사는것보다 이득인지 확인 하기 위하여 
- 낱개를 6개 사는 가이 패키지 1개를 사는 것보다 비싼지 확인한다. 이때 패키지가 이득이 아니라면 바로 낱개로 구매하고 패키지가 이득이 된다면 (패키지최솟값)이 (사야하는 기타줄)*(낱개 최솟값)보다 작은지 확인해준다.
- 만약 작다면 패키지를 사는 것이 이득이므로 패키지를 구매해야한다. dap변수에 패키지 가격을 더하고 사야하는 기타줄은 6개를 감소해준다.(6개를 산다)
- 사야하는 기타줄 개수가 0보다 작거나 같아진다면 반복문에서 빠져나와 dap변수를 출력해준다.

4) 1205등수 구하기
- 등수를 구하는 문제로 랭킹 리스트 점수의 개수와 태수가 받은 점수, 랭킹 리스트에 들어갈수 있는 점수의 개수가 입력된다.
- 이후 랭킹 리스트에 있는 점수들이 입력되는데 이때 태수는 랭킹 몇등이 되는지를 출력하는 프로그램이다. 우선 랭킹 리스트 점수의 개수가 0이라면 태수가 무슨 점수를 
- 받는지 상관없이 1등이므로 1등을 출력한후 프로그램을 종료시킨다. 만약 랭킹 리스트에 점수가 있다면 각각 랭킹 리스트의 점수와 태수가 받은 점수를 비교해 주어야 한다. 
- 랭킹 리스트의 점수가 태수가 받은 점수보다 높다면 태수의 랭킹은 떨어진다. 1등에서 2등으로 2등에서 3등으로 ... ->(rank++)
- 만약 태수의 등수가 랭킹 리스트에 들어갈 점수보다 작다면 랭킹에 들어갈수 없으므로 -1을 출력 한다. 또는 랭킹 리스트에 있는 가장 최솟값 (랭킹에 있는 가장 못한 점수)이
- 태수의 점수보다 크거나 같고 랭킹 리스트에 들어갈 자리가 없다면 -1을 출력해준다.
- 만약 이 조건이 다 아니라면 그때의 태수 등수를 출력해주고 프로그램을 종료시킨다. 간단한 문제였으나 -1을 출력하는 조건을 곰곰히 생각해봐야하는 문제였던것 같다.
