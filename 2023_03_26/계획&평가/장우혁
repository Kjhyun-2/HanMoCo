스택(10828) - 실버4
- stack함수를 이용하여 stack객체 생성
- switch문을 이용하여 입력받은 명령의 문자열을 비교
- push, pop, size, empty : 해당 함수 이용
- top : stack.peek() 함수 이용

괄호(9012) - 실버4
- 변수 x와 y에 각각 "(", ")"으로 초기화
- charAt()함수를 이용하여 문자열을 비교해주기
- if문을 사용하여 ")"가 "("보다 먼저 나오면 No 출력

효정과 새 모니터(20949) - 실버5
- 인덱스 번호를 가져오기위한 배열과 계산결과를 넣어줄 배열을 각각 생성
- -->인덱스 배열은 1, 2, 3, ... 처럼 1부터 인덱스 숫자가 들어가있음
- 모든 계산에 루트와 /D가 포함되어있으므로 w의 제곱과 h의 제곱만 비교해준다.
- 계산결과 배열을 크기별로 정렬하면서 동시에 인덱스배열도 같이 순서를 바꿔줌

설탕 배달(2839) - 실버4
- 배달해야 하는 봉지 무게가 5로 나누어 떨어지는 경우와 그 외의 경우로 나눔
- 5로 나누어떨어지지 않으면 들어야 하는 무게에서 3을 뺀다(3짜리를 든다)
- 5로 나누어 떨어지면 나눈 몫을 출력한다.
- 이 두가지 명령을 while문을 이용하여 반복한다.

2차원 배열의 합(2167) - 실버5
- 2차원 
- 
