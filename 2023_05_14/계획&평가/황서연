1) 10987 모음의 개수
- 모음을 저장해 두는 word 리스트 만들기
- for문을 사용하여 word 길이만큼 반복 
- word의 첫번째 부터 입력받은 문장에 있을 경우 count를 사용하여 cnt 에 더해주기
- cnt 출력

2) 2576 홀수
- 7번 n을 입력받고 if 문 사용하여 2로 나누어 떨어지지 않은 경우(소수)에만 arr배열에 append
- 만약 arr의 길이가 0 일경우 소수가 없는 것 이므로 -1 출력
- 아닐경우 sum으로 합과 min으로 가장 작은 값 출력

3) 10988 팰린드롬인지 확인하기
- 리스트에 단어를 입력 받고 새로운 리스트에 입력 받은 단어를 뒤집은 값을 저장 renersed() 사용
- if문으로 두개의 리스트를 비교해서 동일하면 1, 아니면 0 출력

4) 1436 영화감독 숌
- 666이 들어간 숫자를 찾는 것이므로 처음 시작은 665부터 시작
- 변수에 '666'을 저장해두고 while True를 이용하여 입력 받은 순서 될 때 까지 무한 반복
- 반복문 안에서는 처음 시작 숫자였던 665를 하나씩 증가시키면서 i에 '666'이 1번 이상 들어간 숫자일 경우  cnt를 증가
- cnt와 N이 동일하면 i출력 후 break
