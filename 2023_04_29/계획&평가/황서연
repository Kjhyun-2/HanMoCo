1. 25314 코딩은 체육과목입니다
- 입력받은 숫자만큼 long을 프린트해주는 문제
- 입력받은 숫자가 4로 나누어 떨어지는 경우 4로 나눈 몫을 문자열 'long'과 곱해서 프린트
- 아닐경우 4로나눈 몫에 1을 더한 값을 곱해서 프린트

2. 11653 소인수분해
- 입력받은 숫자를 소인수분해하는 과정을 프린트하는 문제
- 일단 입력 받은 숫자가 1인경우 소인수분해 할 필요가 없음으로 pass 사용
- for문으로 2 부터 입력받은 숫자까지 반복하면서 입력받은 숫자가 i로 나눠질 경우 i를 프린트하고 i로 나눠준다.

3. 2754 학점계산
- 숫자로 학점을 입력받고 알파벳으로 학점을 계산해주는 문제
- if, elif ,else를 활용하여 일정 숫자를 입력받으면 그에 해당하는 알파벳 프린트

4. 11382 꼬마정민
- 큰 수 3개를 더하는 문제
- 다른언어에서는 큰 수를 입력 받으려면 데이터타입을 long long int로 설정해야하지만 파이썬은 int로 입력받아도
계산이 가능하다.
- map을 이용해 한줄에 3개의 큰 수를 입력 받고 더해준다.
