===============================================================================================================================================
4월 29일 (토요일)

1) 25314 코딩은 체육과목 입니다
- 이 문제는 자료형과 관련된 문제이다. long int가 4바이트 정수까지 저장하고 long long int가 8바이트 정수까지
- 저장한다고 알고있는데 그렇다면 12바이트, 20바이트를 저장하려면 앞에 long형을 계속 붙이면 되는것이 아닐까라는 문제이다.
- 처음 N바이트를 입력하고 N바이트를 저장할 수 있는 자료형의 이름을 출력하는것이다.
- 입력은 4의 배수만 들어오기 때문에 입력값에서 4를 빼면서 long을 출력하고 입력값이 0이되면 마지막에 int를 출력한다.

2) 11653 소인수분해
- 정수를 입력받아 입력받은 수를 소인수분해해서 결과를 한줄마다 오름차순으로 출력하는 문제이다.
- 그래서 처음 생각난것이 반복문이였다. 오름차순 정렬이라고 해도 값들을 저장할 필요가 없기때문에 배열로 저장할 필요 또한 없다.
- 나의 코드중 반복문은 for(i=2;i<=입력받은정수; )이렇다 소인수분해중 1은 포함 안되기때문에 초기값은 2를 주고 입력받은 정수까지 돌렸다.
- 이때 증가값은 주지 않았는데 2로 나누어 떨어지면(현재 i값) 증가하지않고 떨어지지않으면 증가하는 형태로 최소값으로 계속 나누며 코드를 짰다.
- if(입력받은정수%i==0) 이면 i값 출력 , 입력받은 정수는 입력받은 정수 나누기 i
- 문제에서 입력받은정수가 1이면 바로 프로그램 종료가 돼야하기 때문에 입력받은 다음 바로 조건문을 통해 1이면 return 0;으로 종료시켰다.

3) 2754 학점계산
- 이러한 문제는 조건문에서 가장 기초적인 문제라고 볼수있다.
- 다만 입력되는 값이 문자열이기 때문에 단순비교로는 조건문 비교가 안될수 있다는 점을 유의하면서 풀었다.
- c++인 경우 string 자료형으로 받아 간단하게 해결되지만 c언어인 경우는 char 배열로 받아야 하기때문에
- 단순하게 if( a == "abc") 이련 형태로는 비교가 되지않는다. 이때 string.h 헤더파일을 불러와 strcmp 함수를 사용하여 비교해야한다.
- 문자열에서 기본적인 문제이나 단순하게 평소 비교처럼 == 연산자를 사용하여 비교한다면 아쉬운 결과를 볼수있다.
- 이러한 문제를 바탕으로 기본기를 탄탄하게 만들어 놔야할것같았다.

4) 11382 꼬마정민
- 이 문제는 입력받은값 a b c를 더해서 출력하는 것이다.
- 엄청 기초적이고 쉬운 문제이지만 이 문제에 질문게시판을 보면 알 수 있듯이 시간초과가 난다, 왜 정답이 안나오는지 모르겠다, 컴파일에러가 난다 등
- 정답률이 생각보다 많지 않은 문제였다. 이유는 입력받는 값의 범위때문이다. 10의 12승까지 입력이 된다고 적혀있는데 별 생각없이
- int형으로 입력받아 출력한다면 원하는 값이 나오지 않는다. 파이썬과 같은 언어는 long long int 같은 자료형없이 int형으로 다 처리되지만
- c같은 경우 꼭 자료형을 신경써서 문제를 풀어야 한다. 그래서 이 문제를 풀때 long long int a,b,c; 형태로 변수를 만들고
- 입력한후 출력했더니 정상적으로 원하는 결과가 나올 수 있었다.
